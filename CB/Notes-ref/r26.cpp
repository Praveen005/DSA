/*

{2,3,5,6,8}
{2,4,5,8}
{1,2,10,12,14,16}

*/

#include<iostream>
#include<bitset>
using namespace std;
#define MAX 30

int main() {
#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
#endif
	bitset<MAX>month;
	int a[] = {2, 3, 5, 6, 8};
	int b[] = {2, 4, 5, 8};
	int c[] = {1, 2, 10, 12, 14, 16};
	int k = sizeof(a) / sizeof(a[0]);
	int l = sizeof(b) / sizeof(b[0]);
	int m = sizeof(c) / sizeof(c[0]);
	for (int i = 0; i < k; i++) {
		mon
	}

}